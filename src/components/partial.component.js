import React, {PropTypes} from 'react';

const Partial = ({partial, changePartial}) => (
  <section style={{backgroundColor: 'lightblue', height: '400px', width: '100px'}}>
  </section>
);

export default Partial;
